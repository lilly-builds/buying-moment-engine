# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# agent worktrees (ce-work parallel dispatch)
/.claude/

# Experiment #1 output — contains real decision-maker names + verified work emails.
# The repo is PUBLIC. The cohort (public business info) is committed; results are NOT.
experiment-1-results.jsonl
experiment-*-results.jsonl

# HubSpot CLI personal access key (never commit)
.hs-pak
hubspot.config.yml

# Enrichment vendor bakeoff and run data: real decision-maker names and verified work
# emails. The repo is PUBLIC. The sanitized narrative doc
# (docs/test-runs/2026-07-13-enrichment-elevation.md) is committed; the raw dumps and the
# full working logs are NOT.
docs/test-runs/*.json
docs/test-runs/*.csv
docs/test-runs/elevate-enrichment.md
docs/test-runs/2026-07-13-coverage-first-production-cron-canary.md

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Screenshot shot list

These are the images the README embeds. Capture them **after the populate run**, on the **live
URL**, so every shot shows **real leads and real briefs**, never an empty state and never a
mockup. Same honesty bar as the written claims: real, built, running UI on real data only.

Save each file in this folder with the exact kebab-case name below (the README already points at
these paths). Then delete the matching `<!-- TODO: Lilly drops this shot -->` line in `README.md`.

| File name | The shot | README beat it sits under | What must be visible |
|---|---|---|---|
| `feed.png` | The feed of real practices at a buying moment, hottest on top | User Story (the AE is handed a constant flow) | Several real named practices, ranked by signal count, freshness badges, headlines showing the buying moment |
| `brief-citation.png` | An open two-tier brief with a source citation clicked open | The timing thesis + "every fact is verifiable" | The brief card with a citation expanded / the source link showing under an underlined claim |
| `scoreboard.png` | The ROI scoreboard with its measured / modeled tags | The ROI beat | Real numbers (meetings, cost per meeting, conversion) each carrying a measured or modeled tag |
| `onboarding-tour.png` | The guided onboarding tour step card | Rollout and training | The step card: gradient orb (purple to health-blue), the one-bold-word instruction, the context chip |
| `integrations.png` | The Connections page (`/integrations`) | Connecting the stack | The three rows: HubSpot (OAuth connect) + Anthropic + People Data Labs (key fields), with their status pills |

## The one "alive" win, if you can grab it

One short **animated GIF** near the top of the README beats any static shot: the guided tour
playing, or a citation being clicked open. If you capture one, save it as `hero-tour.gif` here and
swap it in at the top of the README (there is a TODO marker for it).

## How to capture (quick)

1. Confirm the demo data is populated so nothing is empty.
2. Sign in to the live URL.
3. Grab each shot at a clean moment (a brief with a strong citation, a scoreboard with real
   numbers, a fresh tour state for the step card).
4. Crop to the content; keep it legible at README width.
5. Drop the files here with the exact names above, and remove the TODO lines in `README.md`.
